Project Setup
Running the Project

To launch this project using Docker, open a terminal and navigate to the project's root directory. Then run:

docker compose up --build

This command will build the required Docker images and start all services defined in the docker-compose.yml file.

Available Endpoints
Create a New Student
/NewStudentEndpoint

Creates a new student record.

Create a New Subject
/newSubjects

Creates a new subject.

Get All Subjects
/allSubjects

Returns a list of all available subjects.
